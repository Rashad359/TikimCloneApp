import SwiftUI

struct TestHome: View {
    var body: some View {
        VStack {
            Text("Home")
            
            Spacer()
        }
    }
}

struct NotificationView: View {
    var body: some View {
        VStack {
            Text("Notifications")
            
            Spacer()
        }
    }
}

struct CartView: View {
    var body: some View {
        VStack {
            Text("Cart")
            
            Spacer()
        }
    }
}

struct TestView: View {
    
    var body: some View {
        
    }
}

#Preview("Test") {
    TestView()
}
